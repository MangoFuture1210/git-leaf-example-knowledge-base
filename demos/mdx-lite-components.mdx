---
title: MDX-lite component demo
domain: tools
type: demo
owner: maintainer
last_updated: 2026-08-01
source: git-leaf
canonical: false
source_repository: MangoFuture1210/git-leaf
source_path: docs/mdx-lite-components-demo.mdx
source_revision: 871fb7a
ai_snippet: "[Demo] Git Leaf MDX-lite component preview | DataTable Timeline Chart DecisionBox MetricGrid FlowDiagram | sample data is not a source of truth"
---

# MDX-lite component demo

[Demo index](../README.md)

This fixture exercises every MDX-lite component currently supported by Git Leaf. Its numbers and flows
are sample data for visual validation, not business facts. See the
[MDX-lite renderer reference](https://github.com/MangoFuture1210/git-leaf/blob/main/docs/mdx-lite-guide.md)
for syntax and boundaries.

| Component | What this fixture checks |
| --- | --- |
| `DataTable` | Structured detail, wide-table behavior, and external datasets |
| `Timeline` | Milestones, progress, and risk states |
| `Chart` | Line, bar, grouped, stacked, combo, and external time-series charts |
| `DecisionBox` | Decision, rationale, tradeoffs, reconsideration |
| `MetricGrid` | Small headline metric sets |
| `FlowDiagram` | Main processes and agent handoffs |

## 1. DataTable

Git Leaf chooses search, frozen columns, sticky headers, and Copy CSV from table complexity.

<DataTable title="Document governance fields" columns="field,purpose,required,example,note">
```csv
field,purpose,required,example,note
title,Document title,yes,Git Leaf system architecture,Used for the page title and search
domain,Owning domain,yes,ai,Helps agents route facts
type,Document type,yes,architecture,Determines maintenance expectations
owner,Maintainer,yes,maintainer,Role or account responsible for the document
last_updated,Last maintenance date,yes,2026-07-27,Use YYYY-MM-DD
source,Source system,no,git-leaf,Explains where the material is maintained
canonical,Authority marker,no,false,Only true documents are current authority
ai_snippet,Routing summary,yes,[Demo] ...,Supports search and routing
change_log,Change history,conditional,,Required when repository policy says so
```
</DataTable>

## 2. Timeline

Statuses provide visual cues; prose remains authoritative.

<Timeline title="Git Leaf evolution">
```json
[
  {
    "date": "2026-07-03",
    "title": "Converged on a local knowledge workbench",
    "body": "The repository remains the source of truth and Git Leaf provides the local interface.",
    "status": "done"
  },
  {
    "date": "2026-07-04",
    "title": "Added Source and Live",
    "body": "CodeMirror 6 became the shared source model with short-delay disk writeback.",
    "status": "done"
  },
  {
    "date": "2026-07-05",
    "title": "Moved into a standalone desktop app",
    "body": "Users choose any local Git knowledge repository after launching Git Leaf.",
    "status": "active"
  },
  {
    "date": "2026-07-27",
    "title": "Published English architecture contracts",
    "body": "Contributor-facing technical documentation now uses English as its single source.",
    "status": "active"
  }
]
```
</Timeline>

## 3. Chart

Hovering a chart combines all series for one x value in a shared tooltip.

<Chart title="Knowledge workbench usage" type="line" x="month" series="preview,source,live" unit="sessions" highlight="2026-07" previewLabel="Preview" sourceLabel="Source" liveLabel="Live">
```csv
month,preview,source,live
2026-04,42,0,0
2026-05,68,0,0
2026-06,96,12,0
2026-07,138,58,41
```
</Chart>

<Chart title="New and completed items" type="combo" x="month" series="newItems,completedItems" bars="newItems" lines="completedItems" unit="items" newItemsLabel="New" completedItemsLabel="Completed" highlight="2026-07">
```csv
month,newItems,completedItems
2026-04,152,23
2026-05,120,18
2026-06,150,24
2026-07,486,48
```
</Chart>

<Chart title="Volume and completion rate" type="combo-dual-axis" x="month" series="volume,completionRate" bars="volume" lines="completionRate" rightSeries="completionRate" leftUnit="items" rightUnit="%" volumeLabel="Volume" completionRateLabel="Completion rate" highlight="2026-07">
```csv
month,volume,completionRate
2026-04,152,15.1
2026-05,120,8.5
2026-06,150,9.1
2026-07,486,9.9
```
</Chart>

### External daily dataset

The CSV below is maintained independently as one full daily dataset. Its sidecar manifest defines field
types, meanings, time semantics, and explicit rollups. The same `Chart` and `DataTable` components can
select a date range and switch among day, week, month, and natural-quarter views.

<Chart title="Daily company revenue and expense" dataset="./data/company-daily.dataset.json" type="line" x="period" series="revenue,expense" unit="CNY" revenueLabel="Revenue" expenseLabel="Expense" from="2026-01-01" to="2026-03-31" granularity="month" />

The optional finite `query` block filters typed rows before aggregation; it is data, not an executable
script.

<DataTable title="Weekly company summary" dataset="./data/company-daily.dataset.json" columns="date,revenue,expense,cash,conversion_rate" from="2026-01-01" to="2026-03-31" granularity="week">
```query
{
  "where": [
    {"field": "company_id", "op": "eq", "value": "001"}
  ]
}
```
</DataTable>

## 4. DecisionBox

<DecisionBox title="Keep Git as the knowledge source of truth" status="accepted" owner="maintainer" source="docs/architecture.md">
```csv
label,value
Decision,"Git Leaf is the human interface over a shared Git context repository, not a hosted documentation site."
Rationale,"People need a friendly way to read, edit, sync, and review the same files used by agents."
Tradeoff,"Git Leaf does not provide accounts, remote access, or a public knowledge portal."
Reconsider,"Evaluate a separate hosted surface only if a real use case requires long-lived public access."
```
</DecisionBox>

## 5. MetricGrid

<MetricGrid title="Git Leaf capability summary">
```csv
label,value,delta,note,status
Document modes,3,,Preview Source Live,good
MDX-lite components,6,,DataTable Timeline Chart DecisionBox MetricGrid FlowDiagram,good
Default port,4317,,A compatible service is reused,neutral
Local access,editable,,Desktop and browser service bind to localhost,watch
Editing scope,normal branches,,Detached writes create a protective branch,good
Desktop shell,Electron,,Open any local Git repository,good
```
</MetricGrid>

## 6. FlowDiagram

<FlowDiagram title="Local Git knowledge workflow" note="Main path only; see docs/architecture.md for the complete contract.">
```json
{
  "nodes": [
    {"id": "desktop", "label": "Open Git Leaf", "type": "start"},
    {"id": "repo", "label": "Choose a local Git repository", "type": "action"},
    {"id": "server", "label": "Start or reuse localhost service", "type": "action"},
    {"id": "worktree", "label": "Choose worktree", "type": "action"},
    {"id": "mode", "label": "Preview / Source / Live", "type": "decision"},
    {"id": "protect", "label": "Protect detached writes with a branch", "type": "gate"},
    {"id": "edit", "label": "Write source files", "type": "end"}
  ],
  "edges": [
    {"from": "desktop", "to": "repo"},
    {"from": "repo", "to": "worktree"},
    {"from": "worktree", "to": "server"},
    {"from": "server", "to": "mode"},
    {"from": "mode", "to": "protect", "label": "first write"},
    {"from": "protect", "to": "edit"}
  ]
}
```
</FlowDiagram>

## Acceptance

- All six components render without an MDX component error.
- Both external views expose day, week, month, and quarter controls from one full daily CSV.
- Quarter view uses a natural label such as `2026-Q1` and manifest-defined rollups.
- Chart hover shows the combined tooltip for one x value.
- FlowDiagram renders as static SVG.
- Syntax and new-component policy remain defined by the
  [MDX-lite renderer reference](https://github.com/MangoFuture1210/git-leaf/blob/main/docs/mdx-lite-guide.md).
